query WalletTokens($address: String!) {
  wallet(address: $address) {
    tokens {
      address
      name
      symbol
      decimals
      balance
      wcroPer
      usdcPer
      usdcTotal
    }
  }
}
